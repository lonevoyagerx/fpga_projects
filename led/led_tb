`timescale 1ns/1ps

module tb;

reg clk = 0;
wire led;

top uut (
    .clk(clk),
    .led(led)
);

// clock generation
always #5 clk = ~clk;

initial begin
    $dumpfile("wave.vcd");
    $dumpvars(0, tb);

    #1000;
    $finish;
end

endmodule